<%= form_for :player, url: players_path, html: {id: 'join_game'} do |f| %>
  <p>
    <%= hidden_field_tag :game_id, @game.id %>
    <%= f.text_field :name%>
  </p>
    <%= f.submit 'Join Game', id: "join_game" %>
<%end%>
